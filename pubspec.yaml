name: life_books
description: "人生五册 - 个人数字生命工作台"
publish_to: 'none'
version: 4.0.0+4

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 本地存储（离线）
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5

  # 工具
  image_picker: ^1.1.2
  file_picker: ^8.1.0
  flutter_plugin_android_lifecycle: 2.0.22
  cached_network_image: ^3.4.1
  intl: ^0.19.0
  provider: ^6.1.2
  shared_preferences: ^2.3.4
  uuid: ^4.5.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
